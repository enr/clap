include 'modules:core',
        'modules:uat'
        
rootProject.name = 'clap'

def setBuildFile(project) {
	project.buildFileName = "clap-${project.name}.gradle"
	project.children.each {
		setBuildFile(it)
	}
}

rootProject.children.each {
	setBuildFile(it)
}

